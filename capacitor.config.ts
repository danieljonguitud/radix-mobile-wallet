import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'radix-wallet',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
